# AI 面试陪练 MVP 复盘（RETRO）

> 写于 2026-09-02，MVP 完成并推送 GitHub 之后。
> 这份复盘只记录**真实发生过的事**——每个结论都能对应到一次 commit、一个 bug 或一次用户反馈，不写空泛的道理。
> 目的：下次做 AI 产品时直接照抄。

---

## 一、项目概况

| 项       | 数据                                                                                        |
| -------- | ------------------------------------------------------------------------------------------- |
| 周期     | 约 1 天（从方向确认到推上 GitHub）                                                          |
| 代码     | 21 个源码文件，14 个 commit                                                                 |
| 测试     | 103 个测试（15 个文件），lib 层行覆盖 91%                                                   |
| 功能闭环 | 上传简历（PDF/Word）→ AI 面试官 10 轮追问 → 流式生成报告（真实进度条）→ 重答这题 → 历史回看 |
| 架构     | Next.js 无状态 API + IndexedDB 本地存储，无账号、无数据库、无服务端用户数据                 |
| 成本     | DeepSeek 一场完整面试（10 轮+报告）估算 < 0.05 元                                           |

**方向演变**：项目最初是「AI 口语陪练」（E:\Talk AI 根目录的三份 PRD/设计/技术文档），后来转向「AI 面试陪练」。转向逻辑：我的目标是练**产品思维 + AI 编程 agent 协作能力**，口语陪练的语音链路（ASR/TTS/评测）无法自动化验证，会稀释训练焦点；面试陪练共享同一套产品骨架（场景→对话→反馈→报告→重练），但纯文字让 agent 的强弱清晰可见。**旧项目的调研没有浪费**——腾讯云口语评测的调研结论（自由说模式支持无参考文本评测）已留档，口语陪练仍是 P1 候选。

---

## 二、产品雏形期，最重要的五件事（我的答案）

这是本次最大的认知收获，重要性排序如下：

### 1. 先定「不做什么」，再定做什么

AGENTS.md 里有一节「不做的事」：不做语音、不做账号、不做 RAG、不做部署、不做移动端。这节在执行期**至少三次防止了 scope 膨胀**（例如想做多岗位模板、想做简历文件留存做对比）。雏形期的资源只够一个闭环，"不做清单"比"功能清单"更值钱。

### 2. 单闭环跑通 > 功能数量

MVP 的验收标准是「上传 → 对话 → 报告 → 重答」一条线能真实走完，而不是有多少个功能模块。报告生成从"一次性返回"改造成"流式 + 进度条"花了不少功夫，但它属于**已有闭环的体验补强**，比新增任何功能都值。

### 3. 异常路径是产品的一半

上线前设计的降级矩阵全部被真实触发过：扫描版 PDF → 转粘贴；LLM 超时 → 重试 + 友好文案；报告 JSON 解析失败 → 自动重试 → 降级提示；答不满 2 轮 → 拒绝生成报告。**用户的信任是在出问题时建立的**，不是在一切正常时。

### 4. 等待体验也是体验（本次最典型的产品教训）

用户实测反馈：「报告生成 20-40 秒，没有任何进度信号，不知道生成了多少」。原来的界面只有一个静止 spinner。修复方案不是加个假动画，而是把 /api/report 改成 SSE 流式——LLM 逐 token 输出实时驱动进度条（百分比 + 阶段文案 + 已生成字数）。**教训：任何超过 3 秒的等待都必须有"真实进度"信号，假动画糊弄不了用户。**

### 5. 决策要留痕（AGENTS.md > 记忆）

所有架构决策、密钥红线、质量规则、"不做什么"都写进 AGENTS.md。它的作用：① AI agent 开发时不会破坏决策（例如密钥永不进前端）；② 三个月后回看，知道"当时为什么这么定"；③ 新功能进来时，先问"这违反了哪条约束"。

---

## 三、与 AI 编程 agent 协作的可复用经验

### 1. 质量门禁是驯服 agent 的核心：机器裁定，不靠自证

```
npm run quality = lint + format:check + typecheck + 全部测试
```

规则写死在 AGENTS.md：**任何阶段交付前必须 quality 全绿，否则不算完成**。AI agent 的自证（"我测试过了"）不可信，一条命令客观裁定。本次 103 个测试就是靠这条规则积累起来的。

### 2. 双层验收，缺一不可

| 层       | 判据             | 谁执行     | 抓到过什么                                  |
| -------- | ---------------- | ---------- | ------------------------------------------- |
| 机器认证 | quality 全绿     | agent 自证 | 消息重复 bug（见下）                        |
| 真人实测 | 浏览器全流程走查 | **我本人** | PDF 解析挂了、报告无进度、dev 双 effect bug |

**机器层抓不到的，恰恰是产品最致命的**——三个真实 bug 里两个（PDF、进度）只有真人能发现。

### 3. 三个真实 bug 案例（最有价值的部分）

**案例 1：消息重复 bug（测试抓到，机器层的胜利）**

- 现象：对话页把用户本轮回答既 append 进历史又单独作为 userMessage 传给 API，LLM 收到重复输入。
- 根因：约定边界（messages=历史不含本轮）在两处代码里理解不一致。
- 教训：**AI agent 写代码最容易在"接口约定边界"出错**。对策：类型定义写清楚注释（types.ts 是唯一来源），关键请求体的组装逻辑必须有单测断言"传了什么"。

**案例 2：PDF 解析在真实运行环境挂掉（测试全绿但上线就崩）**

- 现象：vitest 里 PDF 解析测试全绿；用户实测 Word 能解析、PDF 报错。
- 根因：pdfjs-dist 是纯 ESM，被 Next.js webpack 重打包后，它内部动态加载 worker 文件的相对路径失效。
- 修复弯路：第一次修复（手动设置 workerSrc 为 file:// 路径）**反而错了**——被 Next.js 模块 loader 拦截；正确修复是 `serverExternalPackages: ["pdfjs-dist"]` 让库从 node_modules 原生加载 + 不手动干预。
- 教训：**单测环境 ≠ 运行环境**。webpack/bundler 相关的坑（worker、动态 import、fs 路径）单测永远测不到。对策：核心链路必须有一次"起真实 dev server 走 HTTP 验证"的环节；修 bundle 问题时优先"让库保持原生加载"，而不是"教库适应打包器"。

**案例 3：dev 模式 effect 双调用导致报告不生成**

- 现象：报告页有时直接显示"还没有生成报告"，不触发生成。
- 根因：React 开发模式 effect 双调用。第一次 effect 开始生成并置 `generatedRef=true`，第二次 effect 看到 true 误走"未生成"分支。
- 教训：**防重复的 ref 要记录"处理过什么"（如 sessionId），不要用 boolean**——boolean 分不清"正在处理"和"已处理完"。

### 4. 测试策略的取舍（诚实记录）

- 覆盖率口径中途从「lib+组件 ≥80%」改成「lib ≥80%，组件要求关键交互有测试」。原因：HomePage 整页组件测起来性价比低。**这是一个务实的妥协，不是原则**——组件测试该补的还是要补，但雏形期把测试预算投给纯逻辑层回报最高。
- LLM 相关测试全部 mock（不真调网络）：保证测试快、稳定、不花钱。真实 LLM 只在人工验收时调用。
- 真实文件 fixture（一个 PDF 样例放仓库里）比 mock 解析器有效得多。

### 5. 架构决策：无状态 API + 数据全在浏览器

/api/chat 和 /api/report 都不存会话——前端每次把完整历史传上来。好处：① 服务端零用户数据，无账号、无合规负担、无数据库；② API 可测试性极强（断言请求体即可）；③ 换设备/清缓存只是丢历史，不是安全事故。代价见第五节（token 线性增长）。

---

## 四、提示词经验

### 有效的做法（可照抄）

**1. 面试官 prompt：用「规则清单」约束行为，而不是描述人设**

```
【面试规则 - 必须严格遵守】
1. 一次只问一个问题，绝不把多个问题堆在一起。
2. 不评价用户的回答（不说"很好""不太对"），不打断，不提前下结论。
3. 根据用户的回答自然地追问：挖数字和细节、问决策过程、问取舍和复盘。
4. 如果用户答得简短或跑题，追问一次帮助聚焦，不要评判。
```

每条规则都对应一个真实的产品风险（AI 话痨、AI 提前下结论打击用户、AI 一次甩三题用户不知道答哪个）。**写 prompt 前先列出"AI 可能犯的错"，每条错配一条规则。**

**2. 报告输出 JSON：三重保险**

- ① prompt 里给出精确的 JSON 结构示例 + "不要 markdown 代码块包裹"；
- ② 解析层容错：剥掉 ```json 围栏、截取首个 `{` 到末个 `}`、字段校验和 score 越界收敛；
- ③ 失败自动重试一次，重试时附加上下文：「上一次输出无法解析，请只输出纯 JSON」。

实测 DeepSeek 输出 JSON 成功率已经很高，但 ②③ 保证偶发失败用户无感。

**3. 复用通道而非新增接口**

"重答这题"功能需要 AI 针对特定问题开练——没有新增 API，而是把目标问题注入简历前缀：`【本次重练目标问题】xxx\n\n{原简历}`。system prompt 通道复用，零后端改动。

### 不足（下次改进）

- **温度没分场景**：对话用 0.8（自然），但报告生成也应该这样吗？报告要稳定输出 JSON，温度应该更低（0.2-0.5）。目前统一 0.8，是疏忽。
- **prompt 里没有输出长度约束**：面试官有时一句话问得啰嗦。可以加「提问控制在 50 字以内」。
- **system prompt 每轮全量重发**：见下节。

---

## 五、Token 成本：现状、估算与优化清单

### 成本结构（无状态 API 的代价）

每轮对话请求 = system prompt（角色+简历+规则，约 600-800 token）+ 完整历史 + 本轮回答。**输入 token 随轮数线性增长**：

| 时点                   | 单次请求输入估算                 |
| ---------------------- | -------------------------------- |
| 第 1 轮                | ~800 token                       |
| 第 5 轮                | ~2,000 token                     |
| 第 10 轮               | ~3,500 token                     |
| 一场 10 轮面试合计     | ~20,000 token 输入 + ~3,000 输出 |
| 报告生成（单次大调用） | ~5,000 token                     |

按 DeepSeek 价格，一场完整面试 **< 0.05 元**。雏形期可以不管，但要知道结构：**报告是单点大消耗，对话是线性增长**。

### 已做的优化

- 简历文本归一化（normalizeText 压缩空行/空白）
- 流式输出（不省 token，但首字延迟从"全文生成完"降到 ~0.5s，实测体验好）

### 未做但已想清楚的优化（下次照抄）

1. **简历截断**：`summarizeResumeForPrompt(maxChars=2000)` 函数已经写了**但没接线**——`buildSystemPrompt` 直接放了全文。这是本次真实的疏漏，长简历会让每轮请求多付几百 token。教训：**工具函数写了不等于用了，接线和编写要同一步完成**。
2. **历史滚动摘要**：超过 6 轮后，把早期问答压成摘要传（"前 5 轮聊了自我介绍和推荐系统项目，用户提到了 25% 点击率提升"），只保留最近 4 轮原文。
3. **Token 用量打点**：在 /api/chat 响应里带回 usage 字段，前端累计显示"本场已消耗约 X 千 token"。AGENTS.md 早就写了要监控，没落实——**没有度量的成本控制是空话**。
4. **报告 prompt 里限制输出规模**：明确"improvements 每条 suggestion 不超过 80 字"，输出 token 直接减半。

---

## 六、不足与教训（诚实清单）

1. **测试环境盲区**：vitest 全绿 ≠ 能跑。bundler/运行时相关的问题只有真人实测能抓到（PDF 案例）。下次立项第一天就把"起 dev server 走一遍核心链路"写进验收清单。
2. **工具函数写了没接线**（简历截断）：代码存在 ≠ 功能生效。
3. **进度百分比是估算**：报告进度按"已生成字符/1600"估算，报告偏长时会卡在 95% 等结束。可以接受，但要知道它不是精确值。
4. **题量限制的产品反思**：/api/chat 在超过题量后返回 400 拒绝继续。产品上"引导结束"比"强制禁止"更对——用户答上头了想多聊两句，被 API 拒绝很难受。应改为前端提示 + 后端放行。
5. **没有部署方案**：纯本地跑。下次雏形期就应配好一条免费部署路径（Vercel/Cloudflare），让真人测试可以在任何设备进行——本地 localhost 依赖同一台机器，限制了真实用户测试。
6. **组件测试欠账**：HomePage 0% 覆盖，靠的是肉眼。可以接受于雏形，不可接受于迭代期。
7. **没有收集使用数据**：无账号设计下也应该有匿名的关键事件打点（完成率、平均轮数、报告生成成功率），否则产品迭代全靠感觉。

---

## 七、我的思考过程时间线（关键决策链）

记录我提出的几个问题——事后看，每个都在关键节点改变了项目走向：

| 时点   | 我的问题/反馈                                              | 它改变了什么                                                                                   |
| ------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| 方向期 | 「主要想练产品思维和 agent 使用能力」                      | 从口语陪练转向面试陪练，语音链路砍掉                                                           |
| 计划期 | 「代码风格和检查认证的标准定了么？要保证代码能被准确测试」 | 催生了质量门禁（quality 命令）——后来证明这是驯服 agent 的核心                                  |
| 计划期 | 「这个项目有没有加知识库的可能？在哪方面」                 | 明确了知识库三层定位：MVP 轻量注入 / P1 个人数据个性化 / P2 真 RAG，防止一上来就陷进向量库调优 |
| UI 期  | 「怎么给你描述前端样式」→「像 notion.com 一样」            | 学到：给 AI 传达风格要用**参考物+实测色值**，不用形容词；我先去 Notion 官网量了色板再写代码    |
| 实测 1 | 「Word 能解析，PDF 不行」                                  | 抓到测试盲区 bug（webpack/worker），催生"运行时验证"意识                                       |
| 实测 2 | 「报告生成没有进度信号，用户不知道生成多少」               | 催生 SSE 流式 + 真实进度条——本次最有价值的产品改进                                             |
| 收尾   | 「总结可复用的经验和不足」                                 | 就是本文档                                                                                     |

**模式总结**：我的价值主要体现在三个环节——① 立项时定方向和约束；② 关键处提出好问题（质量标准、知识库定位）；③ 真人实测给出具体反馈。写代码本身 AI 包办了，但**这三个环节 AI 替代不了**。

---

## 八、下一次直接照抄的 Checklist

**立项时：**

- [ ] 用一句话写下产品闭环（A → B → C → D），写不出来就还没想清楚
- [ ] 写「不做什么」清单（不少于 5 条）
- [ ] 建 AGENTS.md：产品基线、密钥红线、状态机、验收标准、不做清单
- [ ] 配质量门禁：lint + format + typecheck + test 一条命令，写进 package.json

**写代码时：**

- [ ] 类型定义是唯一来源，接口约定写在注释里
- [ ] LLM 测试全 mock；真实文件放 fixture
- [ ] 防重复 ref 记录 id，不用 boolean
- [ ] 工具函数写完立刻接线，"存在 ≠ 生效"
- [ ] 超过 3 秒的等待必须有真实进度信号（SSE 流式驱动，不做假动画）

**Prompt 时：**

- [ ] 先列"AI 可能犯的错"，每条错配一条规则
- [ ] 结构化输出用三重保险：格式示例 + 容错解析 + 带上下文重试
- [ ] 温度分场景：对话高、报告低
- [ ] 输出加长度约束省 token

**验收时：**

- [ ] quality 全绿（机器层）
- [ ] 起 dev server 真人走全流程，含异常路径（运行层）
- [ ] 体验三问：等待有反馈吗？出错有出路吗？闭环走得完吗？（产品层）

**上线后：**

- [ ] token 用量打点
- [ ] 匿名关键事件统计
- [ ] 一条免费部署路径

---

_下一步（P1 候选）：个人历史数据个性化出题（本地轻量注入）→ 部署上线给真实用户 → 收集匿名行为数据 → 再决定加岗位/知识库。_
