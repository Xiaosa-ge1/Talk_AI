# AI 面试陪练：一个产品从 0 到雏形的实现记录

> 这个项目的起点不是"我发现了一个市场需求"，而是一个训练目标：**我想通过做一个真实的 AI 产品，练产品思维和 AI 编程 agent 的协作能力**。这个起点决定了后面很多选择——本文按故事线记录产品怎么长出来、我在每个岔路口怎么想、哪些做对了、哪些因为约束不严而浪费了资源。

---

## 一、起点：半小时定下的东西，决定了后面的一切

项目开始的第一个动作不是写代码，是回答三个问题。

**1. 做什么？** 在口语陪练（已有完整 PRD，但语音链路重）和面试陪练之间选了后者。理由很功利：语音链路（ASR/TTS/评测）无法自动化验证，会稀释"练 AI coding"的训练焦点；而面试陪练共享同一套产品骨架（场景→对话→反馈→报告→重练），纯文字让 agent 的强弱清晰可见。**选型的第一标准是"能不能支撑我的训练目标"，不是"哪个想法更大"。**

**2. 闭环是什么？** 一句话：上传简历 → AI 面试官 10 轮追问 → 生成报告（维度评估 + 最该改进 3 处）→ 重答这题。写不出这句话之前，不配开始写代码。

**3. 不做什么？** 立项当天写下不做清单：不做语音、不做账号、不做数据库、不做 RAG、不做部署、不做移动端。当时觉得这清单"太保守"，事后证明它至少三次拦住了范围膨胀——比如讨论到知识库时，最后定位成"MVP 轻量注入 / P1 个人数据个性化 / P2 向量 RAG"，没有一上来掉进向量库调优的坑。

> 可复用：立项产出物就三样——一句话闭环、不做清单、把两者写进 AGENTS.md（AI agent 开发时的约束文件）。半小时的事，价值贯穿全程。

## 二、设计期：我自己提出的那个问题，成了项目的地基

计划评审时我提了一个问题："**代码风格和检查认证的标准定了吗？要保证代码能被准确测试。**"

这个问题催生了整个项目的地基——质量门禁：

```
npm run quality = lint(禁 any/禁 console) + format:check + typecheck(TS strict) + 全部测试
```

规则写进 AGENTS.md：**任何阶段交付前必须 quality 全绿，否则不算完成。** 这条规则的本质是：AI agent 的自证（"我测过了"）不可信，用一条命令机器裁定。配套约定：LLM 相关测试全部 mock（不真调网络、不花钱、跑得快），真实调用只在人工验收时发生。

当时我并没有预见到这条规则后来会抓到什么，只是直觉"没有客观验收标准的 AI 协作会失控"。事后看，它是整个项目最值得复用的单一决策——最终积累 103 个测试（15 个文件），lib 核心逻辑行覆盖 91%。

> 可复用：用 AI agent 写代码，第一件事不是让它写功能，是让它把"怎么证明写对了"建起来。

## 三、实现期：代码是怎么长出来的，以及哪里约束得太松

### 3.1 节奏：小步提交，每步全绿

实现按固定节奏推进：IndexedDB store → 简历解析 → LLM 层 → 三个页面 → 报告。每完成一块就跑 quality、git commit（全程 14+ 个 commit）。小步提交对 AI 协作有个隐藏好处：**每次 commit 是干净的回滚点**，改坏了能定位到具体一步；同时每个任务足够小，agent 的出错范围可控。

### 3.2 我约束得太松的四个地方（真实的资源浪费）

这是整个复盘里我最想诚实写的部分。项目能跑，但回头看，**我对系统模块和代码的约束明显低于应有的水平**，造成了几处真实的浪费：

**浪费一：简历没有截断，token 白烧了一整场面试。**
设计时就预料到了这个问题——`summarizeResumeForPrompt(maxChars)` 函数写了，但 `buildSystemPrompt` 放的是简历**全文**，函数从未被调用。长简历让每轮对话的 system prompt 多出几百 token，乘以 10 轮，浪费放大十倍。根因：写函数和接线是两步，没有任何验收项检查"这个优化真的生效了"。**工具函数写了 ≠ 功能生效。**

**浪费二：死代码还在被测试维护。**
报告生成后来改成 SSE 流式，route 直接用 `parseReport` 等纯函数，但旧的 `generateReport`（非流式版本）和它的测试还留在 `lib/report.ts` 里——**生产代码已经不调用它了，测试还在为它买单**。只约束了"新代码要过门禁"，没约束"旧代码要退场"。

**浪费三：模块边界只靠文档约定，不靠工具强制。**
"store 只能在浏览器端用""类型只能从 types.ts 导入"全写在 AGENTS.md 的注释里，没有 lint 规则、没有 server-only 标记、没有目录分层约束挡住误用。小项目靠自觉没出事，但这是运气不是设计。

**浪费四：组件长大后才想起来拆。**
InterviewPage 最终 300+ 行：状态机逻辑、流式处理、后来硬塞进来的"重练模式"分支和 UI 全在一个组件里。合理的做法是第一天就拆出 `useInterviewChat` hook，但"先跑通"的惯性压过了结构约束。

**根因总结**：我对"功能正确"有硬约束（质量门禁），对"结构健康"没有任何硬约束——没有模块分层规则、没有死代码检查、没有"接线生效"验收。**AI agent 很乐意按最低约束交付；你不约束结构，它就给你最平的结构。**

> 可复用：给 AI coding 立的规矩应该包括——① 优化函数写完必须当步接线，验收"生效"而不是"存在"；② 替换实现必须同 commit 删除旧实现和旧测试；③ 模块边界用技术手段强制（lint 规则 / server-only 标记），不靠注释；④ 组件超过 ~200 行必须拆逻辑层。

### 3.3 AI coding 本身的 token 节约

另一个维度的 token：**开发过程中 AI agent 消耗的上下文**。诚实说，这次完全没有度量，但有两个行为被证明省了大量来回：

1. **约束前置**（AGENTS.md + 质量门禁）让 agent 第一次就写对的比例变高，返工对话少——每次返工都要重新加载项目上下文，那才是隐形大头；
2. **小步任务**：一次只描述一个模块，agent 的探索范围小，输出浪费小。

没做但该做的：把"本次任务相关文件"显式给 agent（每次让它自己搜索都是 token）；长项目定期开新会话带摘要续作，避免上下文无限膨胀。

## 四、提示词：这是 AI 产品真正的"产品设计"

这个产品有两类 prompt，设计方法完全不同。

### 4.1 面试官 prompt：先列"AI 会犯的错"，一条错配一条规则

最初的写法是堆人设："你是一位资深产品经理面试官"——结果 AI 一次甩三个问题、动辄评价"答得不错"（打断沉浸）、不结合简历空转。改法不是换形容词，而是列产品风险清单，每条风险配一条硬规则：

```
1. 一次只问一个问题，绝不把多个问题堆在一起。    ← 防：AI 话痨，用户不知答哪个
2. 不评价用户的回答，不打断，不提前下结论。       ← 防：破坏面试沉浸感
3. 根据回答自然追问：挖数字、问决策、问取舍复盘。 ← 防：不结合上下文空转
4. 答得简短或跑题时，追问一次帮聚焦，不评判。     ← 防：AI 说教打击用户
```

**面试官的"人格"来自规则，不来自形容词。** 改完后追问质量明显上来了（自测体验接近真实 PM 面试）。

### 4.2 报告 prompt：结构化输出的三重保险

报告要 LLM 输出严格 JSON，这是最容易翻车的地方：

1. **格式层**：prompt 里给完整 JSON 结构示例 + 明确"不要 markdown 代码块包裹"；
2. **解析层**：容错解析——剥 ```json 围栏、截取首个 `{` 到末个 `}`、字段缺失给默认值、score 越界收敛到 1-5；
3. **重试层**：解析失败自动重发一次，并且**带上失败原因**（"上一次输出无法解析，请只输出纯 JSON"）——带上下文的重试，比盲目重试成功率高。

### 4.3 一个巧思和两个失误

巧思：**"重答这题"没加任何新接口**——把目标问题注入简历前缀（`【本次重练目标问题】xxx\n\n{原简历}`），复用 system prompt 通道，后端零改动。

失误一：**温度没分场景**。对话 0.8 合理（要自然），但报告要稳定输出 JSON，应压到 0.2-0.5——我统一用了 0.8，偶发格式抖动全靠三重保险兜底。
失误二：**没有输出长度约束**。一句"suggestion 不超过 80 字"能省一半输出 token，当时没想到。

> 可复用：prompt 设计 = 风险清单（AI 会怎么犯错）→ 规则映射 → 容错解析 → 带上下文重试；温度、长度这些参数级约束要在写 prompt 时一起定，不是事后补。

## 五、真实使用中遇到的三个问题，和它们各自教会我什么

### 问题 1：LLM 收到了重复的消息（测试抓到的）

- **现象**：组件测试断言请求体时发现，用户本轮回答既被 append 进了对话历史，又单独作为 userMessage 传给 API——LLM 每轮收到两份相同内容。
- **根因**：接口约定（messages=历史、不含本轮）写在 types.ts 注释里，但组装请求的代码理解偏了。
- **教训**：**AI agent 最容易在"约定边界"上出错**。此后每个请求体组装都有测试直接断言"到底传了什么"。这是质量门禁第一次抓到真 bug。

### 问题 2：测试全绿，PDF 解析却挂了（真人抓到的）

- **现象**：我实测第一轮反馈"Word 能解析，PDF 不行"，而 vitest 里 PDF 测试是绿的。
- **根因**：pdfjs-dist 是纯 ESM，被 Next.js 打包后它内部动态加载 worker 的相对路径失效——**测试环境（直接从 node_modules 加载）和运行环境（webpack 打包）不是同一个环境**。
- **修复弯路**：第一次修复（手动指定 worker 文件路径）**反而错了**——被 Next 的模块 loader 拦截；正确解法是 `serverExternalPackages` 让库从 node_modules 原生加载、不干预它的内部机制。
- **教训**：**单测全绿 ≠ 产品能跑**。bundler/运行时相关的问题只有真人实测能抓到；修打包问题的思路是"让库保持原生"，不是"教库适应打包器"。

### 问题 3：报告生成 30 秒，屏幕上没有任何信号（自用抓到的）

- **现象**：结束面试生成报告时只有一个静止 spinner 和一句"通常需要 20~40 秒"——用户不知道进行到哪了，会怀疑卡死。
- **解法**：没做假动画，把 /api/report 从"一次性返回"改成 **SSE 流式**——LLM 逐 token 输出实时推给前端，驱动真实进度条（百分比 + 阶段文案"通读对话→评估表达→逐题诊断→收尾润色" + 已生成字数）。实测 3.5 秒时进度 54%，体验立刻成立。
- **附带收获**：改造中顺手修掉一个隐藏 bug——React dev 模式 effect 双调用导致报告有时不生成（防重复的 ref 从 boolean 改成记录 sessionId）。
- **教训**：**超过 3 秒的等待必须有真实进度信号**。而"真实进度"往往技术上已经存在（LLM 本来就是流式输出），只是你把它扔掉了。

### 暴露了但还没改的（记录在案）

- 题量到上限后 API 直接拒绝继续（400）——产品上应该"引导结束"而不是"强制禁止"；
- 报告进度百分比按预估字数（1600）估算，报告偏长会卡在 95%；
- 断网提示有了，但"进行中对话的断线恢复"没有完整测过。

## 六、运行时成本：一场面试多少钱

按 DeepSeek 价格估算（无状态 API 的代价是历史每轮全量重传）：

| 项                     | 估算                                   |
| ---------------------- | -------------------------------------- |
| 对话第 1 轮输入        | ~800 token（system 含简历全文 + 规则） |
| 对话第 10 轮输入       | ~3,500 token（历史线性累积）           |
| 一场 10 轮面试合计     | ~20,000 token 输入 + ~3,000 输出       |
| 报告生成（单次大调用） | ~5,000 token                           |
| **一场完整面试成本**   | **< 0.05 元**                          |

结构认知：**报告是单点大消耗，对话是线性增长**。雏形期都可以不管，但两件该做的事没做：token 用量打点（AGENTS.md 写了没落实——没有度量的成本控制是空话）、简历截断（见浪费一）。

## 七、结尾：这个项目验证了什么

1. **AI 包办了写代码，但三个环节替代不了我**：立项定约束、关键节点提出好问题（质量标准、知识库定位）、真人实测给具体反馈。三个真实 bug 里两个（PDF、进度）只有真人能发现。
2. **约束的质量 = 产出的质量**。我约束了"功能正确"（103 个测试的门禁），没约束"结构健康"（模块边界、死代码、接线生效），结果两类后果都如实出现了——好在也都记录在了第三节。
3. **雏形期最重要的一件事**：让一个闭环在**真实环境**里跑起来（不是在测试里跑起来），然后让真实使用告诉你下一个最疼的问题是什么。本项目三次最有价值的改进，全部来自"真的用了一下"。

---

_下一步候选：P1 个人历史数据个性化出题 → 免费部署路径（Vercel）→ 匿名行为打点 → 再决定加岗位/知识库。_
